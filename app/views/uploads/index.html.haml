- model_class = Upload
.page-header
  %h1 Excel to XML Conversions
= link_to 'New Conversion', new_upload_path, :class => 'btn btn-primary'
%table.table.table-striped
  %thead
    %tr
      %th Uploaded
      %th Uploaded By
      %th=t '.actions', :default => t("helpers.actions")
  %tbody
    - @uploads.each do |upload|
      %tr
        %td= upload.created_at.in_time_zone.strftime("%m/%d/%Y at %I:%M%p")
        %td= "#{upload.user.firstname} #{upload.user.lastname}"
        %td
          = link_to 'View XML', upload_path(upload, :xml), :class => 'btn btn-xs btn-primary'
          = link_to 'Destroy', upload_path(upload), :method => :delete, :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) }, :class => 'btn btn-xs btn-danger'
= paginate @uploads
